# Handoff — cập nhật 2026-08-26

Điểm vào hiện tại. `handoff.md` ở gốc repo là nhật ký của các phiên cũ (§0 của nó ghi số liệu từ
2026-08-14, đã lỗi thời); đọc file này trước.

> **File này đã lỗi thời một lần trong đúng 24 giờ.** Bản 2026-08-25 nói candidate recall là 74/74 và
> hai case của 092 chỉ là lỗi join — điều đó **đã bị bác bỏ** (xem §"Đính chính"). Nếu sửa gì trong
> repo, cập nhật file này cùng lúc; nó chỉ hữu ích khi còn đúng.

## Trạng thái

| | |
|---|---|
| branch | `m7-m8-audit-provenance` |
| tag | `rc-c31f095` → `c31f0954cfd1af4ad45b8a490b33b71c6cf93b96` |
| RC acceptance | **PASS** — xem `docs/releases/rc-c31f095.md` |
| HEAD | `56f9d78` (2026-08-26) |
| test suite | **1064 pass / 15 fail** — bộ 15 fail là *frozen baseline* từ `dbd65a6` (corpus drift), **không sửa** |
| milestone M7–M12 | CLOSED |
| release / package | RC đã tag; **chưa deploy, chưa release** |

## C1 — CLOSED; partial span preservation is trigger-gated debt

**Completed once at `401a030`:** semantic `20/160` (158 HeadingTopic, 2 BodySentence), span
`20/80` then `partial_timeout`, no batch exception, zero final outputs. Frozen offline source
reconciliation gives 68 `SPAN_TIMEOUT` plus 94 checkpoint `SPAN_RESOLVED`, and zero role losses.
C1.6 replayed the actual validator: **94/94 pointers are `valid` and `eligible`**; they are discarded
only because the partial-timeout wrapper replaces the entire span result with `Uncertain`. Thus the
span deadline/all-or-nothing timeout wrapper is causal for all 162 losses in this run. Do not rerun
001 or increase the deadline yet: 20 completed span batches took a 77.2s checkpoint window, median
3.48s/batch; cost and cross-document evidence are still required for remediation.

**C1.7 offline counterfactual:** preserve the 80 already-completed span blocks, leave the rest
uncertain, then replay existing validator/grounder/alignment. It changes `001` from 0 to **94/162
validated-occurrence coverage**, 56 grounded, and **54 emitted after alignment**; 54 of 55 emitted
blocks map to the exhaustive reviewed gold bridge. Thus these are **54 reviewed emitted outputs lost
under the current wrapper and recovered only by the offline partial-preserve counterfactual**.
Batch/pointer independence is regression-locked; the outer partial-timeout wrapper is the only
all-batch discard. This is a remediation candidate, not a promotion.

**C1.8 completed offline:** `PdfC18PartialTimeoutRecurrenceAuditProbe` scans all persisted artifacts
for the complete shape `partial_timeout + completed valid spans + wrapper discard + material downstream
loss`; timeout alone does not count. The only fully evidenced positive is still **001** (80 checkpointed
span blocks, 94 valid reviewed pointers, 54 reviewed outputs in C1.7's preserve replay). No independent
partial-timeout run has both a validator replay and downstream denominator, and no comparable complete
span-lane checkpoint exists as a material negative control. Therefore **cross-document recurrence is NOT
PROVEN** and production preservation is not promoted. Do not spend a live call just to obtain a second
document; reopen only when an independent instrumented artifact is otherwise justified.

Promotion state: causal/material impact on 001 and batch independence are **PROVEN**;
cross-document recurrence is **NOT PROVEN**; collateral evidence is **INSUFFICIENT**; production
promotion is **BLOCKED**; timeout increase is **NOT JUSTIFIED**. Do not manufacture a canary solely to
obtain another positive. Reopen only for a naturally justified independent checkpointed run, a product
incident with material span-timeout loss, a newly scheduled benchmark that yields a usable control, or
an explicit product requirement for best-effort partial semantic output.

## C2 — composite-boundary census is blocked on lost occurrence identity

B4's aggregate evidence is real: 041 financial had one reviewed composite false accept and 056
textbook had eighteen. It does **not** prove a shared producer. C2.0 searched all reachable Git
history/branches, the current `.verify-build`, the registered worktree, and available historical
source/download directories for B3/B4/B5 artifacts, worksheets, reconciliation/owner reports, and
their `sourceFactId`/`MULTI_HEADING_COMPOSITE` content. Nothing preserved the exact 19 reviewed
occurrences. Commit `6a57759` contains only probes that require external artifact/review paths.

Therefore historical C2.1 is **BLOCKED**: do not reconstruct labels from `1 + 18`, current output, or
legacy artifacts; common producer and a safe split invariant remain **NOT PROVEN**. Reopen only if the
original review packet reappears, an independently justified run naturally retains an occurrence-safe
packet, or a product incident yields one. A later intentionally reviewed workload must be named a
**new composite review population**, never a recovery of B4's 19 cases.

## NEXT.0 — C3 and C4 are blocked by the same evidence-availability gate

The gate checked the only remaining historical owner packets before opening either investigation. All
reachable Git/object paths, available `.verify-build`, the registered worktree, and historical
source/download/temp directories were searched. B5's committed code at `6a57759` is only opt-in
harness code: B5.1 requires external artifact/review paths and B5.2 requires another external
artifact/review pair. It preserves neither input packet nor the 056 checkpoint replication.

Therefore 041's exact twelve TOC/context false-output identities and their source
page/line/block/context trace are **MISSING**, and 056's exact eleven review-question identities
joined to analyst role/confidence/source identity are **MISSING**. B2 bridges are still present but
are heading-gold only, not B4 false-output labels. C3 and C4 are both **BLOCKED**. Do not construct
either population from the `12`/`11` aggregates or from current/legacy outline output. Reopen only
with the original packet, naturally retained equivalent evidence from a separately justified run, or
a product incident; a later independent review must be a **new review population**.

## N0 — new evidence-preserving benchmark population is frozen

N0 does not reconstruct historical B4/B5 labels. Before any new label, output, or model response was
read, `keys/benchmark-n0/manifest.json` froze the A3 selection rule, blind source-first review rule,
retention contract, and taxonomies. The model-free bootstrap selected the lowest unreviewed usable
document in each initial stratum: 003 legal (`160/158` selected/decision-relevant), 029 procurement
(`160/113`), 042 financial (`160/52`), and 057 textbook (`160/100`). Their document SHA-256 values are
pinned in the manifest.

Every future reviewed output must persist source identity/span, representation, route/build
provenance, both lane statuses, role/span checkpoint references, label, and reviewer note. Packets,
labels, bridges, run artifacts, checkpoints, precision packets, and evaluator results must be
committed after each phase; `.verify-build` alone is not authority. N0 ends before review and before
any model call. N1 begins with source review plus the model-free census under this frozen contract.

### N1.1 complete — source-only review packets committed

`eval/benchmark-n0/source-packets/` contains the four blind source packets: 003 has 5,619 lines, 029
has 7,335, 042 has 6,938, and 057 has 24,980. Each carries N0 manifest and document hashes and only
source identity/text plus neighboring source lines. The packet integrity test rejects candidate,
rank, selection, inferred scope/domain, analyst, and validated-output properties.

N1.2's earlier human-gold claim was retracted: all four committed N1.2-S packets are explicitly
`MODEL_ASSISTED_SILVER` and **not human adjudication**. N1.3-S then joined those source occurrences
to the current candidate build: 003 has 128 and 057 has 24 decision-relevant silver occurrences;
029 (3) and 042 (12) are below the frozen live-call threshold of 15 and are not N2-S candidates.

### N1.4 / N2-S preparation complete — still no model call

`eval/benchmark-n0/audit-samples/n1.4-silver-human-audit-source.v1.json` freezes a blind 119-item
human-audit packet: every `MEDIUM` silver heading from 029 (37) and 057 (2), plus ten deterministic
hash-stratified heading and non-heading occurrences for each document. Its separate binding file is
the only place that exposes silver label/confidence, so a future reviewer can work source-first and
agreement can be measured afterwards without mutating N1.2-S.

`eval/benchmark-n0/diagnostics/n1.4-silver-ranking.v1.json` is diagnosis only. It records the
occurrence-safe rank distribution without changing scorer, budget, or labels. The rank loss is large:
the full-candidate p50 is 2,189 for 029, 800 for 042, and 2,131 for 057; this does not authorize a
ranker change before the silver audit and N2-S are complete.

**Incident, corrected:** two Claude Code sessions edited this checkout without coordination. One had
already frozen `eval/benchmark-n0/n2-s/manifest.v1.json` at `semanticConcurrency: 2` before any live
call; the other ran 003 and 057 directly at the CLI default (`semanticConcurrency: 1`), then the
manifest/preflight were retroactively rewritten to `1`/`liveCallsMade: true` to match those runs —
and that rewritten state was briefly committed and pushed as `03d985f`. The rewrite has been reverted
by a new commit, restoring the manifest to its originally frozen `semanticConcurrency: 2` /
`liveCallsMade: false`, not by editing `03d985f` itself. The two concurrency-1 runs are preserved as
quarantined evidence under `eval/benchmark-n0/n2-s/invalid-runs/concurrency-1/`
(`usableForOfficialN2SMetrics: false`) — they are not official N2-S numbers, though 003's run does
reproduce C1.6/C1.7's exact span-lane `partial_timeout`/all-or-nothing-discard mechanism on a second
document, and 057's shows a distinct grounding-stage loss (42/42 validated, 0/24 decision-relevant
occurrences reached `canonicalGroundings`). Full incident record and the new immutability guards
(`PdfN2SContractGuardProbe`) are in `TODO.md`.

### N2-S canonical execution complete

003 then 057 ran at the correct frozen profile (`semanticConcurrency: 2`) after confirming the
worktree was idle and in sync with `origin`. **003 collapses again** - `spanLaneStatus:
partial_timeout`, 0 items - the identical outcome as the earlier off-protocol run, so the concurrency
mismatch was not what caused it; this is a second clean, independent-document instance (after 001 in
C1.6/C1.7) of the span-lane `partial_timeout` all-or-nothing-discard mechanism. **057's semantic lane
succeeds**: both lanes complete, and joined to N1.3-S's decisionRelevant cohort by source line id,
23/24 (95.8%) validate - but 0 reach `canonicalGroundings`.

**Lane A (003) offline counterfactual reopens C1's promotion gate.** Preserving the 88 completed span
blocks instead of discarding all of them recovers 72/128 (56.3%) decisionRelevant occurrences to
emitted - a second material recovery, so cross-document recurrence is now **PROVEN**.

**The 9 emitted-but-unmatched blocks (11.0% headline false-positive rate) are a join artifact, not
model false positives.** All 9 are the opening line of a real silver-reviewed article (`Điều 6, 14,
15, 61, 66, 71, 83, 84, 85`), missing only the wrapped continuation line the recall join's strict
full-containment test requires - and none duplicate another candidate that already fully captured the
same article. 6/7 frozen acceptance-gate criteria now pass; **gate #7 (cross-document regression)
resolves to `INSUFFICIENT_EXISTING_EVIDENCE`**, not a pass or a fail: only 001 and 003 have ever
produced a retained `pdf_hierarchy_facts` run with span-lane evidence anywhere in this repository -
`spanLaneStatus` postdates every other retained checkpoint by construction, and no document is
manufactured to force a verdict. The named next step is freeze-implementation-then-open-N3 (the
untouched fresh holdout), never look-at-N3-then-adjust - not done yet, a decision for whoever owns
promotion.

**Lane B (057), corrected after follow-up: one root cause, not two.** The first pass split the 23
undelivered occurrences into 14 `GROUNDING_VALIDATOR_REJECTION` and 9 `NO_DOCX_SOURCE_ANCHOR`. A
marker-aware grounding fix cleared all 10 three-level-marker cases at the shape gate - but checking
whether the other 4 would then actually align found 0/4 exist verbatim in the DOCX, forcing a check of
all 23. **21/23 are `DOCX_AUTO_NUMBERED_TITLE_ONLY`**: the DOCX paragraph's title matches verbatim and
carries `NumberingId` - the number is Word's auto-generated list numbering, never literal DOCX text,
while the PDF render bakes the visible number into extracted text. The two "sub-owners" were two
pipeline stages one upstream cause happens to hit, not two mechanisms. Remediation candidate: retry
alignment with the marker stripped, specifically against `NumberingId`-bearing DOCX paragraphs - narrow
and specific, not promoted. Ranking remains on hold.

**That candidate's collateral was checked before any implementation.** Across all 19,249 DOCX
paragraphs (2,727 with `NumberingId`), 26 pairs share an exact duplicate title (real textbook
structure - section names recurring across chapters) and 32 `NumberingId`-bearing titles also match a
non-numbered paragraph (nearly all single tokens/symbols too short to ever be a real candidate title).
**The 21 real production targets resolve unambiguously** - verdict is collision-risk-found at the
general-rule level, collision-free in practice for the exact population already proven safe. Any
implementation must stay bounded to already-`HeadingTopic`-validated candidates, not generalized.

Full tables, both probes, and exactly what remains before either fix could be promoted are in
`TODO.md`. Silver/proxy evidence only - N1.2-S is model-assisted, not human-reviewed.

Quy trình vận hành: `docs/release-runbook.md`. Toàn bộ lịch sử quyết định và các debt đóng băng:
`TODO.md`.

## Đính chính — candidate recall 92 không phải artifact

Bản handoff trước kết luận hai occurrence `absent` của 092 là **lỗi join của probe** và trích candidate
`b22` (hạng 434) làm bằng chứng rằng candidate đã tồn tại, nên recall thật là 74/74.

`80389c1` sửa join sang **occurrence identity** đúng cách và chứng minh ngược lại: mỗi occurrence
resolve về đúng một source line duy nhất, và tại dòng đó **không candidate nào `Covers`, cũng không
cái nào `touching`**. Đây là **candidate-construction gap thật**.

Chỗ sai nằm ở chi tiết đáng nhớ: `b22` là **candidate id từ một lần chạy cũ**. Id được gán theo thứ tự
discovery và **không ổn định qua các thay đổi code** — trích một id cũ làm bằng chứng cho trạng thái
hiện tại là sai phương pháp. Chỉ **re-resolve trên run hiện tại** mới là bằng chứng.

## Việc đang dở — Extractor quality benchmark

Mục tiêu hẹp: **extractor có tìm và xác nhận được heading không, trước khi các debt output/scope làm
mất chúng.** Không phải benchmark sản phẩm end-to-end.

### A1 — candidate recall + selection (model-free, 8 document)

| doc | reviewed | full | partial | absent | rank ≤ 160 | recall |
|---|---:|---:|---:|---:|---:|---:|
| 054 | 23 | 23 | 0 | 0 | 18 | 100% |
| 092 | 37 | 35 | 0 | 2 | 35 | 94.6% |
| 032 | 8 | 8 | 0 | 0 | 2 | 100% |
| 091 | 6 | 6 | 0 | 0 | 1 | 100% |
| 001 | 761 | 761 | 0 | 0 | 165 | 100% |
| 028 | 113 | 84 | 4 | 25 | 1 | 74.3% |
| 041 | 62 | 61 | 0 | 1 | 15 | 98.4% |
| 056 | 46 | 32 | 6 | 8 | 15 | 69.6% |
| **ALL** | **1056** | **1010** | **10** | **36** | **252** | **95.6%** |

```
BENCH_A1_REPORT=<path> [BENCH_A1_DEBUG=1] dotnet test tests/DocxHeaderExtractor.Tests \
  --filter "FullyQualifiedName~PdfExtractorQualityBenchmarkProbe"
```

**Selection tách riêng khỏi recall** và không được gộp: 252/1010 vào tới budget 160. Khoảng cách đó là
first-loss của tầng ranking, không phải của extractor.

### B2–B5 — đã xong (3 live OpenRouter call)

Gold cross-domain đóng băng tại `f514ddc` (`keys/benchmark-round-2/manifest.json` giữ frozen profile).

**B3 — conditional semantic recall**

| doc | decision-relevant | validated reviewed | recall |
|---|---:|---:|---:|
| 001 legal | 162 | 0 | **0/162 (0%)** |
| 028 procurement | 1 | N/A | cohort không đủ, không gọi |
| 041 financial | 15 | 15 | 15/15 |
| 056 textbook | 15 | 15 | 15/15 |

Chứng minh semantic-stage loss **chỉ ở 001**; chưa phân định owner là analyst proposal, span mapping
hay validator. Không mở điều tra 001 trong vòng này.

**B4 — validated precision, census đầy đủ, không sampling**

| doc | reviewed | TRUE | TOC | composite | non-heading | precision |
|---|---:|---:|---:|---:|---:|---:|
| 041 | 29 | 16 | 12 | 1 | 0 | **55.2%** |
| 056 | 46 | 17 | 0 | 18 | 11 | **37.0%** |

Đây là precision **theo từng document**, không phải precision gộp của sản phẩm.

**B5 — precision first-owner audit:** không có owner chung. 041 lệch theo hình dạng TOC/context; 056
tách thành representation-boundary (18) và analyst-role (11 câu hỏi ôn tập được analyst gán
`HeadingTopic` 0.9–1.0). **Không mở remediation nào.** Replication của 056 cho 45 thay vì 46 output —
ghi nhận là instability, **không chạy lần thứ ba**.

### Chưa xong / chưa quyết

- Không có milestone remediation nào đang mở. Mọi debt vẫn trigger-gated (bảng dưới).
- Nếu mở rộng benchmark: **screen model-free trước** → chỉ review gold cho document chứng minh usable
  → rồi mới gọi model. Bài học M11-A0.
- Quyết định cũ *"A2 chạy 4 document hay chỉ 054+092"* **đã bị vượt qua** — phiên sau chọn đường khác:
  dựng gold mới cho 001/028/041/056 thay vì đo trên sample của 032/091.

## Ràng buộc bắt buộc giữ

Chi tiết trong `TODO.md`. Ba gate promotion, mỗi loại thay đổi dùng gate riêng:

1. **Accuracy remediation** — observed loss → causal owner → generalize → collateral → promote.
2. **Fail-closed hardening** — không cần material loss lịch sử, nhưng cần: authority invariant rõ,
   trạng thái permissive reachable, điều kiện từ chối deterministic, valid-path không đổi, không
   mutation khi reject, coverage cả hai nhánh.
3. **Observability/provenance** — cần chứng minh một fact *đã có authority* bị mất ở evidence
   boundary khiến hai execution khác nhau trở nên không phân biệt được. *"More metadata is better"
   không phải trigger.*

Và:

- **Không sửa production để khớp gold.** Gold phải occurrence-safe trước.
- **Absence of a marker không phải positive evidence.**
- **Đo signal trên đúng population nó phân loại**, đo cả benefit lẫn collateral.
- **Một biến mỗi lần đo.**
- **Observed defect ≠ causal owner ≠ safe remediation.**
- **Debt đã ghi không tự động thành backlog item** — cần trigger mới mở lại.
- **Evaluation không được dựng lại computation của production** (không implementation thứ hai).
- Milestone chẩn đoán được phép kết thúc với **0 production change** — M10 đã như vậy.

## Debt đóng băng — chỉ mở khi có trigger

| debt | trigger mở lại |
|---|---|
| appendix latch không có exit (032/091/092) | promote như **source-fact correctness** với collateral gate riêng, **không bao giờ** bằng lập luận recall |
| quote latch: điều kiện mở/đóng đọc hai tập ký tự rời nhau | corpus dùng nháy cong cho thấy loss đo được |
| `DetectTocBlockIds` im lặng trên 092 | material loss mà **chỉ** nó giải thích được |
| marker depth hỏng do extraction chèn khoảng trắng | có thứ downstream định dùng marker depth |
| selection tiêu budget cho candidate output sẽ loại | biện minh bằng **model cost/throughput**, đo như cost |
| no-marker score ceiling 0.44 | corpus review cho material loss, hoặc product requirement, hoặc một fact sẵn có được chứng minh là discriminative |

Đặc biệt: `TableLike` remediation **đã đóng** — causal trên 092 nhưng **không generalize** (1/14
ngoài 092), và bị chặn bởi TOC owner.

## Bẫy vận hành — đọc trước khi chạy bất cứ gì

1. **`dhx.cmd` chọn `out-vulkan` trước `bin/Release`** và chạy binary *đầu tiên tồn tại*, không phải
   mới nhất. Lúc viết file này `out-vulkan` **cũ hơn HEAD 403 commit**. Xác nhận revision thực chạy,
   hoặc gọi thẳng `dotnet run --project src/DocxHeaderExtractor.Cli -c Release -- …`.
2. **`--openrouter` là bắt buộc.** Thiếu nó CLI **âm thầm** dùng Local llama.cpp và vẫn success.
   Artifact sẽ ghi `backend: Local` trung thực — run đó **không phải** OpenRouter evidence.
3. **`validatedHeadings: 0` tự nó không phải failure.** Đọc `semanticLaneStatus` trước;
   `null` = **unknown**, không phải `complete`.
4. **Không chạy lại model để test determinism.** Boundary determinism đã khoá ở M11-B2; replay dùng
   artifact đã sinh.

## Lệnh hay dùng

```bash
# build
dotnet build DocxHeaderExtractor.sln -c Release

# test (kỳ vọng 1064 pass / 15 frozen fail)
dotnet test tests/DocxHeaderExtractor.Tests/DocxHeaderExtractor.Tests.csproj --nologo -v q

# live run (A2) — đúng một lần mỗi document, không retry
dotnet run --project src/DocxHeaderExtractor.Cli -c Release -- \
  pdf-hierarchy-facts <file.docx> --openrouter --openrouter-model qwen/qwen3.5-9b --out run.json

# replay offline từ artifact, không gọi model
M11_B42_ARTIFACT=run.json M11_B42_REPORT=out.txt \
  dotnet test tests/DocxHeaderExtractor.Tests --filter "FullyQualifiedName~PdfLiveCanaryReplayProbe"
```

Các probe chẩn đoán khác đều nằm trong `tests/DocxHeaderExtractor.Tests/*Probe.cs` và chỉ chạy khi có
biến môi trường tương ứng — chúng không tham gia suite mặc định.

## Gold hiện có

```
keys/occurrence-bridge/
  001_Bo_luat_Dan_su_91-2015-QH13.occurrence-bridge.json
  028_WB_RFB_Works_Without_Prequal_2017.occurrence-bridge.json
  041_IBRD_Financial_Statements_June_2025.occurrence-bridge.json
  054_IBRD_Information_Statement_FY25.occurrence-bridge.json
  056_OpenStax_Business_Law_I_Essentials.occurrence-bridge.json
keys/benchmark-round-2/manifest.json          frozen profile của vòng B2–B5
keys/rebased/054_….v3-occurrence-reviewed.key
eval/manual-labels/092-short-numbered-line-labels.v1.json      125 dòng (37 outline)
eval/manual-labels/m105b-cross-document-review-labels.v1.json  80 dòng, 032+043+063+091
```

Đây là **toàn bộ** occurrence-reviewed gold của dự án. Muốn benchmark document mới thì phải review
thêm — và phải screen trước khi review.
