# Handoff 2026-08-28

## Completed

Phase B live canary was run on `diagnosis/span-production-canary`, commit
`6d4700d`, and pushed. It used exactly one OpenRouter call with
`qwen/qwen3.5-9b` for document 004. No retry, VLM, production change, or
remediation was performed.

Artifacts:

- `eval/accuracy/span-production-canary.v1.json`
- `docs/accuracy/span-production-canary.md`

The run completed its semantic and span lanes. The checkpoint contains 20
semantic batches and 21 span batches, with 81 resolved span blocks and 3 null
blocks. Frozen and canary selection each contain 160 candidates and 181 source
line IDs, but their raw hashes differ because Unicode source identities differ;
they are not treated as the same downstream experiment.

## Evidence limits

The checkpoint has no timing decorator, `startedAt`, latency, or HTTP outcome
fields. Therefore span call-started/cancelled/faulted counts, latency statistics,
H3, and H4 are not proven. H1 architecture gap remains proven from Phase A, but
its operational impact is not applicable to this run because the span lane
completed. Semantic timeout counters are not HTTP evidence.

## Next work

1. If causal evidence is required, add a diagnostic timing decorator/replay
   harness without changing the request contract or retrying the completed
   provider run.
2. Do not change timeout, concurrency, retry, batch size, ranking, candidate
   construction, validator, or production authority in this workstream.
3. Open 004 remediation only after timing evidence and same-experiment selection
   identity are established.

Provider calls: `1`. VLM calls: `0`. Production code changed: `false`.
