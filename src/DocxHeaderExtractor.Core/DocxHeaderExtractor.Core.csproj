<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Phần thuần của RuntimeDescription phải kiểm được mà không nạp 4,4 GB trọng số. -->
    <InternalsVisibleTo Include="DocxHeaderExtractor.Tests" />
    <!-- Historical evaluation is isolated from the normal Core runtime path. -->
    <InternalsVisibleTo Include="DocxHeaderExtractor.Eval" />
  </ItemGroup>

</Project>
